import { consoleSandbox } from '@sentry/core';
import { withSentryConfig as withSentryConfig$1 } from './withSentryConfig/index.js';

let hasWarned = false;
function withSentryConfig(nextConfig, sentryBuildOptions = {}) {
  if (!hasWarned) {
    hasWarned = true;
    consoleSandbox(() => {
      console.warn(
        "[@sentry/nextjs] Importing `withSentryConfig` from `@sentry/nextjs` is deprecated and will stop working in v11. Import it from `@sentry/nextjs/config` instead:\n  import { withSentryConfig } from '@sentry/nextjs/config';"
      );
    });
  }
  return withSentryConfig$1(nextConfig, sentryBuildOptions);
}

export { withSentryConfig };
//# sourceMappingURL=deprecatedWithSentryConfig.js.map
